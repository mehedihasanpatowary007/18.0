<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="zencore_dashboard_icon_own_records_rule" model="ir.rule">
        <field name="name">Zencore Dashboard Icon: own records only</field>
        <field name="model_id" ref="model_zencore_dashboard_icon"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
